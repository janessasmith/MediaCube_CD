"use strict";
angular.module('websiteToBeCompiledWordModule', [])
    .controller('editingCenterWebsiteWordCtrl', ['$scope', '$compile', '$window', '$modal', '$filter', '$location', '$anchorScroll', '$stateParams', '$q', '$state', '$validation', '$timeout', 'trsHttpService', 'trsconfirm',
        function($scope, $compile, $window, $modal, $filter, $location, $anchorScroll, $stateParams, $q, $state, $validation, $timeout, trsHttpService,  trsconfirm) {
            initStatus();
            initData();
            /*初始化状态*/
            function initStatus() {
                
            }
            
            /**
             * [initData description]初始化数据
             * @return {[type]} [description]
             */
            function initData() {
                
            }
            
        }
    ]);
